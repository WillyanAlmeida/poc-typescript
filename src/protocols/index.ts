export type Person = {
    id: number
    firstName: string
    lastName: string
}

export type Count = {
    count: number
}

export type PersonName = {
    firstName: string
    lastName: string
}

export type Errors = {
    type: (string | number),
    message: string
}
    